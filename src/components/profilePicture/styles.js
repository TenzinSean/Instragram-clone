import { StyleSheet } from  'react-native'; 

const styles = StyleSheet.create({
    container: {
        margin: 5,
        borderRadius: 40,
        borderWidth: 3,
        borderColor: "#ae22e0",
        marginTop: 40
    },
    image: {
        borderRadius: 40,
        borderWidth: 1,
        borderColor: '#ffff'
    }
});


export default styles;